#include "functions.h"
int main(int argc, char *argv[]){
initialize(argc, argv);
//tokenize(pathname);
//algorithm start. 
  //step 1: open disk for read. Get file descriptor 'fd' as a dev number.
  //clear fd and dev;

  return 0;
}
